# 로컬 Vite dev 전용. Docker/nginx 게이트 뒤 프로덕션 빌드에서는 비우거나 이 줄을 제거(동일 출처 /api 사용).
# 브라우저에 http://127.0.0.1:18080 (오케스트레이터) 직통을 넣지 마세요 — docker에선 호스트에 안 열리고 CORS도 다릅니다. Vite가 /api→게이트(:18580)로 프록시합니다.
# VITE_API_BASE_URL=
# VITE_VOICE_API_BASE_URL=
